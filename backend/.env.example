# Backend Environment Configuration

# MongoDB Atlas connection string (optional)
# If not set, the backend will use local file storage
# Get your connection string from: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://dhia:dhia@cluster0.vdntp.mongodb.net/?appName=Cluster0

# Server port (default: 5000)
PORT=5000

# Public directory path (optional - defaults to ../public)
# PUBLIC_DIR=/path/to/public

# Data directory path for presets (optional - defaults to <PUBLIC_DIR>/presets)
# DATA_DIR=/path/to/data

# ========== Google Drive Configuration ==========
# Path to your Google Drive service account credentials JSON file
# Download this from Google Cloud Console > APIs & Services > Credentials
# GOOGLE_CREDENTIALS_PATH=./credentials.json

# Google Drive folder ID where audio files will be stored
# Get this from the URL when you open the folder in Google Drive
# e.g., https://drive.google.com/drive/folders/YOUR_FOLDER_ID
# GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here
